# .env.example - template showing required environment variables
# Copy this file to `.env` and fill in the secret values. Do NOT commit
# your real `.env` file to version control.

# OpenAI API key (secret). Example: sk-xxxx... Leave empty in the example.
OPENAI_API_KEY=

# Logging configuration
LOG_LEVEL=INFO
LOG_DIR=logs
LOG_FORMAT=text
# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-api-key-here

# Optional: Set a different base URL if using a proxy
# OPENAI_BASE_URL=https://api.openai.com/v1

# Optional: Set default model
# OPENAI_MODEL=gpt-4o-mini

# Logging Configuration (Enterprise-grade logging)
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log directory (logs are rotated automatically)
LOG_DIR=logs

# Log format: text (human-readable) or json (for log aggregation tools)
LOG_FORMAT=text
